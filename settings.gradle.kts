pluginManagement {
    repositories {
        jcenter()
        aliyunGradlePluginPortal()
        gradlePluginPortal()
    }
}

rootProject.name = "kotlinx-bimap"

enableFeaturePreview("STABLE_PUBLISHING")
